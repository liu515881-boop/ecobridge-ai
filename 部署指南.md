# EcoBridge AI 部署指南（Vercel 免费版）

## 部署前准备

### 1. 注册 Vercel 账号
- 访问：https://vercel.com
- 用 GitHub 账号登录（或邮箱注册）
- 免费版额度：足够测试使用

### 2. 安装 Vercel CLI（可选）
```bash
npm i -g vercel
```

---

## 部署步骤

### 方法 A：Vercel 网页部署（推荐）

**步骤 1：上传代码到 GitHub**
```bash
# 进入项目目录
cd C:\Users\86178\.openclaw\workspace\ecobridge-ai

# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 创建 GitHub 仓库并推送
# 访问 https://github.com/new 创建仓库
# 然后推送
git remote add origin https://github.com/[你的用户名]/ecobridge-ai.git
git push -u origin main
```

**步骤 2：Vercel 部署 Frontend**
1. 访问 https://vercel.com/new
2. 导入 GitHub 仓库
3. 选择 `frontend` 文件夹作为根目录
4. 点击 Deploy
5. 获得域名：`ecobridge-ai.vercel.app`

**步骤 3：部署 Backend（需要 Node.js 服务器）**

⚠️ **注意：** Vercel 免费版不支持长期运行的 Node.js 服务器

**替代方案：**
- **方案 1：** 使用 Vercel Serverless Functions（需要改造代码）
- **方案 2：** 使用 Render.com（免费 Node.js 服务器）
- **方案 3：** 使用 Railway.app（免费 Node.js 服务器）

**推荐：使用 Render.com 部署 Backend**

1. 访问 https://render.com
2. 注册账号
3. 创建 New Web Service
4. 连接 GitHub 仓库
5. 设置：
   - Root Directory: `backend`
   - Build Command: `npm install`
   - Start Command: `node server.js`
   - Environment Variables: 添加 `DASHSCOPE_API_KEY=sk-add7b84ebbfb4183b15876a8a4c25fc5`
6. 点击 Deploy
7. 获得域名：`ecobridge-ai-backend.onrender.com`

**步骤 4：更新 Frontend API 地址**

编辑 `frontend/app.js`：
```javascript
// 修改这行
const API_BASE_URL = 'http://localhost:3000';

// 改为
const API_BASE_URL = 'https://ecobridge-ai-backend.onrender.com';
```

重新部署 Frontend。

---

### 方法 B：本地测试（快速）

**步骤 1：启动 Backend**
```bash
cd C:\Users\86178\.openclaw\workspace\ecobridge-ai\backend
npm install
npm start
```
Backend 启动在：http://localhost:3000

**步骤 2：打开 Frontend**
- 直接双击打开 `frontend/index.html`
- 或者用浏览器访问：http://localhost:3000（需要启动前端服务器）

**步骤 3：测试上传识别**
- 上传一张废金属图片
- 等待 AI 识别
- 查看结果

---

## 测试清单

- [ ] Backend 启动成功
- [ ] Frontend 可以访问
- [ ] 图片上传正常
- [ ] AI 识别返回结果
- [ ] 结果展示正常
- [ ] 错误处理正常

---

## 费用说明

| 服务 | 免费版额度 | 是否够用 |
|------|------------|----------|
| Vercel Frontend | 100GB 流量/月 | ✅ 够用 |
| Render Backend | 750 小时/月（可能休眠） | ⚠️ 测试够用 |
| 通义千问 API | 按次收费（约 0.03 元/次） | ✅ 测试很便宜 |

---

## 下一步

1. 本地测试通过
2. 部署到 Vercel + Render
3. 准备预热文案
4. 社交媒体发布

---

**联系：** 有问题随时问虾小米 🦐
